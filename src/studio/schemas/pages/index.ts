import event from './event';
import exhibition from './exhibition';
import landing from './landing';
import publication from './publication';

export default [...landing, ...exhibition, ...event, ...publication];
