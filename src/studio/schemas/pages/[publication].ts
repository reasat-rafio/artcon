import { BsFillPostageFill } from 'react-icons/bs';

const publication = {
  name: 'publication',
  type: 'document',
  icon: BsFillPostageFill,
  fields: [
    {
      name: 'name',
      type: 'string',
    },
  ],
};

export default publication;
